// src/app/careers/assessment/layout.js
export const metadata = {
  title: 'Field Sales Agent — Written Assessment | Carfix-Connect',
  description: 'Timed written assessment for the Carfix-Connect Field Sales Agent role. Complete all sections within the allocated time.',
  robots: { index: false, follow: false },
}

export default function AssessmentLayout({ children }) {
  return children
}