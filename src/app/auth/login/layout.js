// src/app/auth/login/layout.js
export const metadata = {
  title: 'Sign In',
  description: 'Sign in to Carfix-Connect to manage your vehicles, track maintenance, and book garage services.',
}

export default function LoginLayout({ children }) {
  return children
}